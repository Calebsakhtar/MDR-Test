#include <vector>
#include <algorithm>
#include "../headers/DesignClasses.h"

bool A_dominates_B(const MDR::Design& A, const MDR::Design& B, const size_t& metric_id) {
	double perf_val_A = 0;
	double perf_val_B = 0;
}
